-set_user
%h2= t '.title'
=render 'users/form', user:current_user
.container
  .row
    .col-md-6
      %h3= t '.remove.title'

      %p #{button_to t('.remove.btn'), registration_path(resource_name), data: { confirm: t('.remove.confirm')}, method: :delete}

      = link_to "Back", :back
