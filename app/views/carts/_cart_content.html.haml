.container.cart-content.text-center
  -if @order.orders_items.empty?
    %h2= t '.empty'
  -else
    %table.table.books.text-left
      %thead
        %tr
          %th
          %th= t '.book'
          %th= t '.price'
          %th= t '.quantity'
          %th= t '.total'
          %th
      %tbody
        -@order.orders_items.each do |item|
          =render 'carts/cart_row', item:item
        %tr
          %td.text-right{colspan:4}
            =render 'coupons/form', coupon: @order.coupon
          %td{colspan:2}
            %b.cart-total
              $#{current_order.total}
        %tr
          %td.text-right{colspan:5}
          %td
            =link_to t('.make-order'), checkout_path, class: 'btn btn-success'
